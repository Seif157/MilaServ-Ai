# Phase 1 — Pre-implementation · Business responses

Answers to `docs/needs/business/phase1/phase1needs.md`. One section per request, same ID.

- **Business** fills in *Answered by* and *Answer*, and sets the request to `DONE` in the needs file
  (or sends it to Seif — see U4)
- **AI** runs the check, fills in *Verified by* and *Result*, and sets it to `VERIFIED` only on a match

---

### U1 — The business owner's name

Answered by:    —
Answer:         —

Verified by:    —
Result:         —

---

### U2 — The deadline

Answered by:    —
Answer:         —   (YYYY-MM-DD)

Verified by:    —
Result:         —

---

### U3 — D1 tenancy: database per client, or shared?

Asked of:       —   (client contact)
Answered by:    —
Answer:         —   ("database per client" or "shared")

Verified by:    —
Result:         —

---

### U4 — How the business owner writes their responses

Answered by:    —
Answer:         —   ("web editor" or "via Seif")

Verified by:    —
Result:         —
